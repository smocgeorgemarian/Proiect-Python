"""
Package for factories of FileManager implementations. Currently,
containing only FileManagerConfigurator and instantiating managers
based on string args (usually parsed command-line args) and their
internal representation mapped using the pattern that every string respects
"""